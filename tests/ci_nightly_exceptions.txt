# =============================================================================
# ci_nightly_exceptions.txt -- tests the nightly may not execute normally
# =============================================================================
# The nightly DISCOVERS tests by glob. This file is the only way a discovered
# test is treated differently, and it lists ONLY exceptions -- never the normal
# tests. If this file ever grows to resemble a test manifest, the discovery
# contract has been inverted and the nightly has stopped protecting anything.
#
# Format:  <path><TAB or spaces><class>  # reason
#
# class is one of:
#   external-private   needs a private repository this public CI cannot clone.
#                      Runs in the `external-deps` job, reported, NOT fatal.
#   external-network   needs a third-party service to be up. Runs in the
#                      `network-tests` job, reported, NOT fatal.
#
# There is deliberately no "skip" class. A test that nobody runs and nobody
# reports on is indistinguishable from a deleted test, and 21 files already
# reached that state once in this repository.
#
# tests/ci_nightly_registry_check.R fails the build when an entry names a file
# that no longer exists, so a stale exception cannot quietly outlive its cause.
# =============================================================================

# -----------------------------------------------------------------------------
# Requires the mysterynpi package (2026-09 migration: the canonical name keys
# moved from a source()-ed mufflyt/isochrones checkout into the mysterynpi
# package). mufflyt/mysterynpi went PUBLIC on 2026-09-05, so the
# external-deps job now installs it and RUNS three of these informationally;
# the normalization suite additionally needs the gitignored person-level
# roster (T12 fails loudly without it, by design) and so stays runner-blocked
# either way. To promote the other three to blocking, install mysterynpi in
# the r-suite job and delete their entries here.
# -----------------------------------------------------------------------------
tests/test_amcb_name_normalization.R    external-private  # canonical name keys (mysterynpi)
tests/test_cycle12_names_territories.R  external-private  # canonical name keys (mysterynpi)
tests/test_cycle4_ct_apportionment.R    external-private  # canonical name keys (mysterynpi)
tests/test_mysterynpi_contracts.R       external-private  # the consumer-side contract suite

# Stage 2 still needs the private mufflyt/isochrones checkout for the
# canonical rank_one_to_one() -- that function did NOT move to mysterynpi.
tests/test_isochrones_stage2_rank_one_to_one.R  external-private  # canonical rank_one_to_one()

# -----------------------------------------------------------------------------
# Downloads a CMS Facility_Affiliation file at run time, so it depends on
# data.cms.gov being up. Stays informational for that reason and no other.
#
# UPDATED 2026-08-16. The reason used to be "that URL returns 404", which was
# true but was not an outage: CMS rotates the resource id on every refresh, and
# the pinned _1782750576 had been superseded by _1785521778 on 2026-07-31.
# resolve_facility_affiliation_url() now resolves the current URL from the
# provider-data catalog, and the test passes.
#
# I briefly retired this entry on the strength of that. That was wrong. A fixed
# URL does not make the test hermetic -- it still needs CMS to answer -- and
# promoting it to blocking would put a third party's uptime in front of every
# nightly. The entry stays; only its reason changed.
#
# Worth noting the shape anyway: an exception can outlive its stated cause, and
# the only thing that surfaces that is trying the excepted test again.
# -----------------------------------------------------------------------------
tests/test_match_npi_to_hospitals.R     external-network  # needs data.cms.gov
