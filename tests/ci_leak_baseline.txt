# =============================================================================
# ci_leak_baseline.txt -- tracked files the leak guard flags but should not
# =============================================================================
# This list used to hold 69 person-level artifacts that were committed before
# tests/ci_leak_guard.R existed. All of them are now untracked. What remains is
# five FALSE POSITIVES: files the guard flags on a column name or a filename,
# which carry no identifiable midwife.
#
# THE RULE IS UNCHANGED: this list may shrink, never grow. If CI sends you here
# to add a path, the artifact should be gitignored and rebuilt instead. The only
# reason to add a line is a genuinely new false positive, and it needs the same
# one-line justification the entries below carry.
#
# Untracking never removed these files from git history. Every existing clone
# still has them; a history rewrite is a separate decision with its own cost.
# =============================================================================

# 537 members of Congress from the public unitedstates/congress-legislators
# dataset. Names and birthdays of sitting public officials, published
# deliberately by the source. A pipeline INPUT, not an output about midwives.
data/congress/legislators_current.csv

# Two example rows with the invented NPI 1234567890. A schema template, not data.
data/delivery_activity_TEMPLATE.csv

# One row of run counters (voters streamed, cohort n, matches). Flagged on the
# word "voter" in the filename, not on anything it contains.
artifacts/ohio_voter_license_ages_provenance.csv

# One row recording that the Florida extract is still pending. Same false
# positive as the Ohio sidecar above.
artifacts/florida_voter_license_ages_provenance.csv

# Header only, zero rows. The columns are person-level; the file describes
# nobody. Left tracked so the expected schema stays visible.
artifacts/no_final_npi_provenance.csv
