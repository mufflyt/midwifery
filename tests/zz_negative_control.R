#!/usr/bin/env Rscript
# TEMPORARY. A deliberate H4 violation used to prove that `Scientific gate`
# turns red and GitHub refuses the merge. This branch is never merged and is
# deleted immediately after the demonstration. mn_render is already defined at
# top level in tests/lib_manuscript_numbers.R.
mn_render <- function(...) NULL
